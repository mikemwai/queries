show parameter control_files;
show parameter memory;

show parameter spfile --- Check the SPFILE (location) being used by the DB instance
show parameter pfile --- Check the PFILE (location) being used by the DB instance

show parameter sga_target --- Check the SGA target size from the memory
show parameter pga_aggregate_target --- Check the PGA aggregate target size from the memory

show parameter nls_date_format; --- Check the NLS date format being used by the DB instance